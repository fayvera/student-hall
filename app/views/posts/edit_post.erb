
<div class="navbar">
<a href="/students/<%=current_user.slug %>">Home</a>
<a href="/posts">All Publications</a>
<a href="/courses">All Courses</a>
<a href="/logout" class="right">Log Out</a>
</div>

<h2>Edit:</h2>

    <form method="POST" action="/posts/<%= @post.id %>">
        <input type="hidden" name="_method" value="patch">
        <input type="text" name="title" value="<%= @post.title %>"><br></br>
        
        <input list="courses" name="course" id="course" placeholder="Course">
        <datalist name="course" id="courses">
        <% @courses.each do |course| %>
            <option name="course" value="<%= course.name %>" ><br>
        <% end %>
        </datalist><br></br>

        <textarea name="content" rows="10" cols="30"> <%= @post.content %> </textarea>
        <br>
        <input type="submit" value="Edit">
    </form>

